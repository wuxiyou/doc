* 方法重载
>> 方法名相同，但必须参数个数不同(参数顺序不同也可以，但不建议)

```
class Tree {
   Tree () {
        System.out.println("aaaa");
   }
   
   Tree(int a) {
     System.out.println(a);
   }
}
```

-------------------------------------------------------